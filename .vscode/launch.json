{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run CK3 Mod DevKit",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "${workspaceFolder}/../.."
      ],
      "outFiles": [
        "${workspaceFolder}/dist/**/*.js"
      ],
      "preLaunchTask": "${defaultBuildTask}"
    }
  ]
}
